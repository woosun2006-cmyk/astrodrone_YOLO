 ## gcs program 계획안

  ### 보여야 할 내용
  1. 비행 로그 실시간으로 보기(json, csv파일 형식으로 픽스호크와 통신) - 얘는 cmd창에 띄우도록 (이건 MAVLink표준 메시지임)
  2. yolo-live.cpp 화면 받아 보기
  3. heartbeat, 현재 고도, yolo model의 target인식 여부, 픽스호크의 비행 모드, arm/disarm 등등의 다양한 정보가 실시간으로 떠야 함. - 얘는 화면 구성 필요

  ### 패키지 복구 시스템 필요
  1. 쏘게 되는 내용의 일부가 손실 될 가능성이 있음. -> 보낼 때 복구 파일을 함께 보내서 받을 때는 심각한 오염문제가 없는 상황에는 정상적으로 gcs에서 비행log를 받고 있어야 함.
  2. 만약 보내는 패키지 오염 문제가 심각하다면, 일시적으로 log를 보내지 않음. 
  ### 대상
  jetson에서 통신 패키지를 구성해서 보냄
  노트북에서 jetson이 보낸 패키지를 풀어서 받음.
  즉, jetson이 화면 구성에 있어서 필요한 모든 리소스를 감당하지 않음.
  화면을 그리는 것은 노트북에서 담당하고, jetson은 json형태의 파일로만 줌. 
